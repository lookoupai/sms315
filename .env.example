# SMS避坑指南 - 环境变量配置示例
# 
# 使用方法：
# 1. 本地开发：复制此文件为 .env.local
#    cp .env.example .env.local
# 
# 2. VPS部署：复制此文件为 .env
#    cp .env.example .env
# 
# 3. Vercel部署：在Vercel Dashboard中设置环境变量，无需创建文件

# 管理员密码（生产环境请在Vercel Dashboard中设置）
VITE_ADMIN_PASSWORD=your_strong_password_here

# Supabase配置（如果使用Supabase认证）
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# 其他配置
VITE_APP_NAME=SMS避坑指南
VITE_APP_VERSION=1.0.0